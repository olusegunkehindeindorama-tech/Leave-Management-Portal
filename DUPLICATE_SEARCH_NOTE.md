# Important

`SearchEmployees.gs` defines `searchEmployees` (ID + name).

If Apps Script reports a duplicate function error, **delete** the old `searchEmployees` function from `Code.gs` (keep the one in SearchEmployees.gs).
